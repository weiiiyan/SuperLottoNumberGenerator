## 问题

1. LottoEngine作为一个生成号码的算法应该放到外层，接口放到用例层；因为生成算法应该属于细节；
2. LottoGroup作为大乐透号码的抽象实体，应该包含校验自己的关键业务逻辑；

## 改进方向

1. LottoTicket作为一张大乐透彩票的抽象实体，应该支持不同号码数量与是否追加；
2. LottoGroup作为大乐透号码的抽象实体，可以支持复式。
3. 可以增加与LottoEngine平级的自选方式（属于新用例，随机是点击到生成，自选是选择到生成）；